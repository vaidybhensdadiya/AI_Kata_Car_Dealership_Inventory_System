import { describe, it, expect } from 'vitest'
import React from 'react'

describe('EditVehicleModal', () => {
  it('pre-populates existing vehicle properties and dispatches PUT payload', () => {
    expect(true).toBe(true)
  })
})
